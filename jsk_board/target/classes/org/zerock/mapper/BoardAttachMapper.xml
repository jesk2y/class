<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.BoardAttachMapper">
<insert id="insert">
	insert into tbl_file(uuid, uploadpath, filename, bno)
	values(#{uuid}, #{uploadpath}, #{filename}, #{bno})
</insert>

<delete id="delete">
	delete from tbl_file where uuid = #{uuid}
</delete>

<select id="findByBno" resultType="org.zerock.domain.BoardAttachVO">
	select * from tbl_file where bno = #{bno}
</select>

</mapper>